[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "memagent"
version = "1.8.0"
description = "类比人脑的持续学习 Agent 记忆框架（纯标准库，零第三方依赖）"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["memagent*"]
